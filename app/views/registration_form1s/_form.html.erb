<%= form_with(model: registration_form1, local: true) do |form| %>
  <% if registration_form1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(registration_form1.errors.count, "error") %> prohibited this registration_form1 from being saved:</h2>

      <ul>
      <% registration_form1.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.text_field :phone %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
