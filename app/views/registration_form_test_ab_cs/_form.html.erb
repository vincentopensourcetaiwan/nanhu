<%= form_with(model: registration_form_test_abc, local: true) do |form| %>
  <% if registration_form_test_abc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(registration_form_test_abc.errors.count, "error") %> prohibited this registration_form_test_abc from being saved:</h2>

      <ul>
      <% registration_form_test_abc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.text_field :phone %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
